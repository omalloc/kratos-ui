import { PageContainer } from '@ant-design/pro-components';

const HomePage: React.FC = () => {
  return (
    <PageContainer ghost>
      <div>Configs Page</div>
    </PageContainer>
  );
};

export default HomePage;
